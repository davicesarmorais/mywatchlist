# import sqlite3

# con = sqlite3.connect("data.db")
# print("Opened database successfully")

# con.execute("CREATE TABLE user(id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, email TEXT, password TEXT)")

# con.execute("CREATE TABLE title_by_user(id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, title_id INTEGER)")

